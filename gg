#!/bin/bash
# gg: git add, commit, and push
# Usage: source this file, then use: gg "commit message"

gg() {
    git add .
    git commit -m "$1"
    git push
}

# If script is executed directly (not sourced), call the function
if [ "${BASH_SOURCE[0]}" -ef "$0" ]; then
    gg "$@"
fi
